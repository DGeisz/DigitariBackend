## Req
#if(!$ctx.stash.get("newReport"))
    #return
#end

{
    "version": "2018-05-29",
    "statements": [
        "UPDATE posts SET report_count = report_count + 1 WHERE id = :PID",
    ],
    "variableMap": {
        ":PID": $util.toJson($ctx.args.pid),
    }
}

## Res

{}