query GetFollowers($tid: ID!) {
    followers(tid: $tid) {
        entities {
            tid
            sid
            time
            imgUrl
            name
            entityType
        }
        nextToken
    }
}