query GetFollowing($sid: ID!, $nextToken: String) {
    following(sid: $sid, nextToken: $nextToken) {
        entities {
            tid
            sid
            time
            imgUrl
            name
            entityType
        }
        nextToken
    }
}